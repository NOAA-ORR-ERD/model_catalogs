name: model_catalogs
channels:
  - conda-forge
dependencies:
  - python=3.8  # this is required for the dev tools/precommit stuff
  - alphashape
  - appdirs
  - cf_xarray
  - dask
  - datetimerange
  - extract_model
  - h5netcdf
  - intake
  - intake-xarray
  - netcdf4
  - numpy
  - pandas
  - pip
  - shapely
  - siphon
  - xarray
