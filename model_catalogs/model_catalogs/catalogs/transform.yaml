name: Generic dataset transform skeleton
description: This is used in `make_user_catalog` to transform the user-requested model.

sources:
  name:
    driver: model_catalogs.process.DatasetTransform
    args:
      targets:
        - null
      transform: "model_catalogs.process.add_attributes"
      transform_kwargs:
        axis: null
        standard_names: null
